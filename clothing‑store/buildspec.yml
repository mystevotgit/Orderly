version: 0.2

phases:
  install:
    commands:
      - npm install
  build:
    commands:
      - cd clothing‑store
      - npm run build  # Builds the project
artifacts:
  files:
    - '**/*'
  base-directory: dist  # Directory with output files (e.g, React app)
